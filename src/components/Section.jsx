import { TimeAndDate } from './TimeAndDate';

import './Section.css';

export const Section = () => {
    return (
        <section className='section'>
            <TimeAndDate />
        </section>
    )
}